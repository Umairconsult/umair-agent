# =====================================================================
#  ADD THIS BLOCK TO THE BOTTOM OF YOUR settings.env
#  (keep everything you already have - just paste this underneath)
#  Then copy the WHOLE file again into the GitHub secret SETTINGS_ENV
#  (GitHub > Settings > Secrets and variables > Actions > SETTINGS_ENV > Update).
# =====================================================================

# ---------- MORE GEMINI KEYS (recommended) ----------
# Your first key stays on the GEMINI_API_KEY= line higher up. Put your other keys here, one per line.
# If one key runs out of free quota or stops working, the agent switches to the next one by itself.
GEMINI_API_KEY_2=
GEMINI_API_KEY_3=
GEMINI_API_KEY_4=
GEMINI_API_KEY_5=

# ---------- NEW FEATURES (already set, change only if you want) ----------
# Overture Maps = a second, much bigger free source of businesses (yes / no)
USE_OVERTURE=yes
# The agent audits first, and pauses finding new leads while this many leads still wait for an audit
MAX_UNAUDITED_BACKLOG=60
# Blog drafts the agent writes for your website per week (0 = none)
BLOG_POSTS_PER_WEEK=2
BLOG_VOICE=conversational, practical and direct, like an experienced consultant explaining things to a busy business owner

# ---------- YOUR WORDPRESS WEBSITE (optional) ----------
# Lets the agent send the blog posts you APPROVED to your website. Leave WP_USER and WP_APP_PASSWORD empty to skip.
# How to get them: WordPress admin > Users > Profile > "Application Passwords" > name it "AI Agent" > Add > copy the password.
WP_URL=https://umairconsult.com
WP_USER=
WP_APP_PASSWORD=
# draft = saved as a DRAFT on your site (safest)      publish = goes live immediately
WP_PUBLISH_MODE=draft
